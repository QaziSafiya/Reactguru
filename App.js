import React from "react";
import ReactDOM from "react-dom/client"

const h1= React.createElement("h1",{},"hello react guru ji  and after seeing the react lectures of guru ji i fall in love of guruji 🚀");

const h2= React.createElement("h2",{}," 🚀 parcel");

const root = ReactDOM.createRoot(document.getElementById("root"))

root.render(h1);

root.render(h2);